import React from 'react'
import ReactDOM from 'react-dom'
import Extend from '../components/Extend'

const extendElement = document.getElementById('extends')
ReactDOM.render(<Extend />, extendElement)
