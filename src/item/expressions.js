import React from 'react'
import ReactDOM from 'react-dom'
import Expression from '../components/Expression'

const expressElement = document.getElementById('expressions')
ReactDOM.render(<Expression />, expressElement)
